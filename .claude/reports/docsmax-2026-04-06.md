# Docsmax Report — 2026-04-06

**Session**: MIDI device cohesion + DJ tab audit (aside)
**Compile**: Clean — EXIT_OK, 0 errors, pre-existing warnings only
**Tests**: 4,384 (unchanged — no new test files this session yet)
**Version**: 5.2.0 (patch in progress)

---

## Files Changed This Session

### `lib/sound_forge/midi/device_manager.ex`
- **Added** public API: `list_physical_devices/0`
- **Added** private: `normalize_device_name/1`, `coalesce_by_prefix/1`, `group_by_physical_device/1`
- `list_devices/0` unchanged — still returns raw ports for functional callers (midi_live, monitor subscribe)

### `lib/sound_forge_web/live/handlers/midi_handlers.ex`
- `safe_list_midi_devices/0` now calls `list_physical_devices()` — display callers get grouped physical devices
- `client_midi_devices_updated` rewritten: atom-keyed maps; merges client Web MIDI devices that aren't already in physical list; eliminates string-key/atom-key template access mismatch
- `toggle_midi_monitor_listen` unsubscribe loop fixed: iterates `port_ids` list instead of single `port_id`

### `lib/sound_forge_web/live/components/app_header.ex`
- Device row sub-label: shows `{direction} · {N} ports` when `port_count > 1`, else `{direction} / {type}`
- `midi_tooltip_text/1` unchanged — still uses `& &1.name` (works with atom-keyed physical devices)

### `lib/sound_forge_web/live/dashboard_live.html.heex`
- MIDI settings panel device row: same port-count sub-label pattern

### `lib/sound_forge_web/live/components/control_surfaces_settings.ex`
- MIDI tab device row: same port-count sub-label pattern

### `lib/sound_forge_web/live/settings_live.ex`
- `refresh_midi_devices` handler now calls `list_physical_devices()` and assigns result

### `lib/sound_forge_web/live/midi_live.ex`
- `mount/2`: added `physical_devices = DeviceManager.list_physical_devices()`
- AppHeader assign changed from raw `devices` to `physical_devices`
- Functional assigns (`@devices`, `@device_research`, `default_controller`) still use raw `list_devices()`

---

## Architecture Delta (patch)

### Before
```
DeviceManager.list_devices()         → 14 raw Core MIDI ports
  → @midi_devices in all live views  → broken template (atom vs string keys)
  → MIDI badge shows "14"
  → SMC Pad invisible (listed as "SINCO", unrecognizable)
```

### After
```
DeviceManager.list_physical_devices() → 2 physical devices (MPC Live II, SINCO SMC-PAD)
  → @midi_devices in display contexts → consistent atom-keyed maps
  → MIDI badge shows "2"
  → Both devices visible by display name everywhere
DeviceManager.list_devices()          → still available for port-level functional code
```

### Normalization Rules
| Input port name | Normalized |
|----------------|-----------|
| `MPC Live II MPC Public` | `MPC Live II` |
| `MPC Live II MPC Private` | `MPC Live II` |
| `MPC Live II MPC MIDI Port A` | `MPC Live II` |
| `SINCO SMC-PAD-Private` | `SINCO SMC-PAD` |
| `SINCO SMC-PAD-Master` | `SINCO SMC-PAD` |
| `SINCO Port 3` | `SINCO` → coalesced into `SINCO SMC-PAD` |

---

## Session Continuation Files Changed (2026-04-06)

### `lib/sound_forge_web/live/components/dj_tab_component.ex`
- Transport row merged inline with track title (play + title/artist + position + SYNC/LEARN/TAP/BPM)
- Reprocess banner: shown when `deck.track.download_path` nil or `File.exists?` false
- `reprocess_track` handler: creates `ProcessingJob` + enqueues `ProcessingWorker` via Oban
- Compact metronome pill under master (was full `p-4` row)

### `assets/js/hooks/dj_deck.js`
- `_attachKnobDrag()`: vertical pointer drag for `.djknob` elements (sensitivity 1.5px/unit)
- `_attachFaderDrag()`: vertical pointer drag for CSS-rotated channel faders (sensitivity 0.8px/unit)

### `lib/sound_forge_web/live/components/daw_tab_component.ex`
- Verify File button always visible in header
- `handle_event("verify_file")`: `Music.get_track!/1` + `File.exists?/1`
- `file_verified` / `file_verify_error` assigns via `assign_new`
- Outer container `flex-1 min-w-0 overflow-hidden` on `#daw-tab` and `#daw-preview-container`

### `lib/sound_forge/accounts/user_settings.ex`
- Added `field :auto_verify_on_library_open, :boolean, default: false`

### `priv/repo/migrations/20260406100000_add_auto_verify_on_library_open_to_user_settings.exs`
- Migration applied — `alter table(:user_settings) do add :auto_verify_on_library_open ...`

### `priv/python/demucs_runner.py`
- EEXIST fix: `islink` check + `os.unlink()` before `os.makedirs` for stale `/tmp/demucs` symlink

### `lib/sound_forge/midi/device_manager.ex` (commit 4eb6c15)
- `list_physical_devices/0`, `normalize_device_name/1`, `coalesce_by_prefix/1`, `group_by_physical_device/1`

---

## Session Continuation 2 (2026-04-06 — commits 24e3244, 9b68987)

### `assets/js/hooks/dj_deck.js` (commit 24e3244)
- **Fixed** syntax error: `DjDeck` is an object literal, not a class — added missing commas after `_restorePersistedValues` and `_attachFaderDrag` closing braces
- **Removed** duplicate JSDoc block left from prior fader drag addition
- esbuild now builds clean: `mix assets.build` EXIT_OK

### `lib/sound_forge_web/live/components/dj_tab_component.ex` (commit 9b68987)
- **Added** `dj_layout: "classic"` and `show_browser_panel: false` to `mount/1`
- **Added** `set_layout` handler: switches between `"classic"` and `"traktor"` layouts
- **Added** `toggle_browser_panel` handler
- **Added** render dispatch: `def render(%{dj_layout: "traktor"} = assigns)` → `render_traktor/1`
- **Added** layout switcher buttons to classic layout header
- **Added** `render_traktor/1`, `traktor_deck_full/1`, `traktor_deck_compact/1`, `traktor_mixer/1` (378 insertions)
  - 4-deck layout: A/B full-size + C/D compact
  - Center mixer: 3-band EQ, gain, filter, FX send per channel, channel faders, crossfader
  - FX rack row with 3 effect slots per side
  - Browser panel (toggleable)
  - `assign_new` for deck_a/b/c/d mapping from deck_1/2/3/4

## Pending Work

| Item | Target File | Status |
|------|------------|--------|
| Deck C/D backend wiring (classic layout) | `dj_tab_component.ex` | Pending |
| Chef AI GENERATE smoke test | `dj_tab_component.ex` | Pending |
| Traktor template fidelity improvements | `dj_tab_component.ex` | In progress |

---

## PR

**#22**: `ralph/control-surface-mapper-2026-04-05` → `main`
5 commits — ready to merge
